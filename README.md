```sh
npm i
# Edit the config.js file to set the time range

## Creates the /articles/*.json files
node fetch-article-urls.js

## Gets the image paths
## /image-urls/*.json
node scrape-articles.js
```
